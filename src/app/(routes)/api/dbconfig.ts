// import { GetDBSettings, IDBSettings } from '@/sharedCode/commons';

// export default function handler(req, res) {
//   const dbSettings = GetDBSettings();
//   res.status(200).json(dbSettings);
// }
